<form method="post" action="{{ path('bidon_delete', {'id': bidon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bidon.id) }}">
    <button class="btn">Delete</button>
</form>
